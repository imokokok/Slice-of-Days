# 用户原画接入 · 2026-09-24

将用户指定的两层住处接入 residence；A 从左侧外楼梯进入楼上，B 从右侧门进入楼下，共用海风路 17 号的门口信箱。房间画面、家具互动与出门流程保留。旧 dorm 外观仍用于另一处建筑。

用户指定的深色帽子、红色耳机立绘绑定唱片店老板 xanni，显示名仍是 Xanni。共用既有居民位置与对白，不创建第二个 NPC，进入和离开房间也不会复制角色。室内的交談距离与人物尺寸对应；太靠近时主角会短暂后退留出空间，老板站位不变。户外沿用场景光照，室内沿用原有角色显示规则。

原 JPEG 按原文件字节保留。运行时只将连通的纸底与明确标记的楼梯、阳台空隙变透明；保留鞋子上的白色和浅色墙面。材质处理缓存跨场景复用，首次 Xanni 遮罩约 0.9 秒，随后进店不重复处理。脚下、尺寸与建筑地线通过同一组图像坐标定位。

## 验证

- 源码素材与交互回归：31 项，0 失败。
- 原有 home_interiors：47 项，0 失败，实际 E 进门、进入各自房间、桌面与出门流程。
- 导出的 Windows PCK：相同素材与交互回归 31 项，0 失败。
- 检查了两人住处外观、室内保留情况、唱片店老板与对话截图；另外打开打包资源对应的真实 Godot 窗口核对显示。
- 使用 --isolated-save，测试不会修改玩家正式存档。

测试命令：Godot --path . --script tests/integration/test_supplied_home_and_owner.gd -- --isolated-save。
PCK 检查额外传入 --main-pack .runtime/windows/Solmere.pck；截图输出环境变量 SOLMERE_QA_CAPTURE_DIR 必须指向可写绝对目录。

本次范围是这两张原画及相关进出与对话流程。没有重画其他人物、UI、房间或小游戏。
截图：ui_reference_review/supplied-art.html。来源与打包文件哈希：supplied_art_20260924.json。
